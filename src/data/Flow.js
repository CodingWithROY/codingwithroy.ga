import React from 'react'
import Navbar from '../components/Navbar'
import Page from '../components/Page'
import Sidebar from '../components/Sidebar'
import flow from '../images/flow.png'

const Flow = () => {
    return (
        <>
            <Navbar />
            <Sidebar />
            <div className='dataContent'>
                <div id="flow">
                    <h1 className="heading">Flow of C Program</h1>
                    <div className="explain">
                        <div className="py-4" id="how_compiler_works">
                            <h2 className="heading2">
                                How compiler will execute your C program?
                            </h2>
                            <div className="explain">
                                <ul className="pl-8">
                                    <li>You have your C program ready to be executed i.e. simple.c </li>
                                    <li>Now, your program is sent to preprocessors to convert it into their respective values.</li>
                                    <li>Now you have an expandable source code generated by preprocessors i.e. simple.i</li>
                                    <li>Then compiler compiles your code and converts it into assembly code i.e. simple.s</li>
                                    <li>The assembly code is then sent to the assembler that assembles it and converts it into the object code i.e. simple.obj</li>
                                    <li>Then linker links it to the library or header files. Now, it transformed into an executable code i.e. simple.exe</li>
                                    <li>The executable code is then sent to the loader that loads the code into the memory followed by the execution of the code. The output is then sent to the console, after execution</li>
                                </ul>
                            </div>
                            <img className="dataImage" src={flow} alt="" />
                        </div>
                    </div>
                </div>
            </div>
            <Page previous={`overview`} next={`installation`} />
        </>
    )
}

export default Flow